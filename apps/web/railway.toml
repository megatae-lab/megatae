[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter web build"

[deploy]
startCommand = "pnpm --filter web preview --port $PORT --host"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
