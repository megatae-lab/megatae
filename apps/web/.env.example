# API
VITE_API_URL=http://localhost:3001

# Método de pago por transferencia bancaria (cuentas + upload de
# comprobante). "false" lo oculta del checkout y de /admin/configuracion sin
# borrar el código — "true" cuando se quiera reactivar.
VITE_TRANSFERENCIA_HABILITADA=true
